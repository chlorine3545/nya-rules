name: Nya Nya Nya

on:
  schedule:
    - cron: "0 0 * * *" # 每天早八撸一次猫 =w=
  workflow_dispatch:

jobs:
  get-food:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3.5.3

      - name: Get Files
        run: |
          set -e
          curl -O https://raw.githubusercontent.com/Loyalsoldier/clash-rules/release/gfw.txt

      - name: Upload gfw.txt as artifact
        uses: actions/upload-artifact@v4
        with:
          name: gfw-artifact
          path: ./gfw.txt
          retention-days: 1

  nya-1:
    needs: get-food # 明确依赖关系
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3.5.3

      - name: Set up Python
        uses: actions/setup-python@v4.7.0
        with:
          python-version: "3.12"

      - name: Download gfw.txt
        uses: actions/download-artifact@v4
        with:
          name: gfw-artifact
          path: ./

      - name: Make keywords.txt
        run: |
          set -e
          python3 -u ./scripts/keywords.py keywords.txt

      - name: Upload keywords.txt
        uses: actions/upload-artifact@v4
        with:
          name: keywords-artifact
          path: ./keywords.txt
          retention-days: 1

      - name: Make gfw.txt
        run: |
          set -e
          python3 -u ./scripts/simplify.py gfw.txt

  nya-2:
    needs: [get-food, nya-1] # 更清晰的依赖关系
    runs-on: ubuntu-latest
    steps:
      - name: Generate release tag
        id: tag
        run: echo "tag=$(date +'%Y%m%d%H%M%S')" >> $GITHUB_OUTPUT

      - name: Download gfw.txt
        uses: actions/download-artifact@v4
        with:
          name: gfw-artifact
          path: ./

      - name: Download keywords.txt
        uses: actions/download-artifact@v4
        with:
          name: keywords-artifact
          path: ./

      - name: Create Release
        uses: softprops/action-gh-release@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: ${{ steps.tag.outputs.tag }}
          name: Release ${{ steps.tag.outputs.tag }}
          files: |
            gfw.txt
            keywords.txt
          draft: false
          prerelease: false
